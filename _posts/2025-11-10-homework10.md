---
title: "Counting Process Simulation: Poisson"
date: 2025-11-18 06:31 +0300
categories: [Stochastics, Probability]
tags: [Lambda, Time, Intervals]
author: collinskirui
math: true
---

# Foundations of Probability — Homework

## 1. Interpretations of Probability

Probability can be understood in several ways depending on the context.  
Different interpretations emphasize different aspects — symmetry, long-run frequency, belief, or geometry — but all of them can be expressed within a single mathematical framework.

---

### 1.1 Classical Interpretation

The classical viewpoint applies when all outcomes are treated as equally possible.  
If an experiment has finitely many symmetric outcomes, the probability of an event \(A\) is

$$
P(A)=\frac{\text{number of favorable outcomes}}{\text{number of possible outcomes}}.
$$

This is natural for games involving dice, cards, or coins.  
Its limitation is that it only works when the sample space is finite and a symmetry argument actually exists.

---

### 1.2 Frequentist Interpretation

The frequentist picture defines probability as the long-run frequency of an event.  
If an experiment is repeated \(n\) times and event \(A\) occurs \(N_n(A)\) times, we observe the empirical frequency

$$
\frac{N_n(A)}{n}.
$$

The probability is seen as the limiting value

$$
P(A)=\lim_{n\to\infty}\frac{N_n(A)}{n},
$$

assuming this limit exists.  
This connects directly to laws of large numbers, but it doesn’t handle unique events and depends on an idealized infinite process.

---

### 1.3 Bayesian Interpretation

The Bayesian interpretation treats probability as a measure of uncertainty or belief.  
Probabilities depend on what information an agent has at the moment.  
When new evidence \(B\) is observed, beliefs are updated by Bayes’ rule:

$$
P(A\mid B)=\frac{P(B\mid A)P(A)}{P(B)}.
$$

This interpretation allows probabilities for one-time events, but introduces priors, which can vary from person to person.

---

### 1.4 Geometric Interpretation

In continuous settings with uniform sampling, probability is proportional to geometric size:

$$
P(A)=\frac{m(A)}{m(\Omega)},
$$

where \(m\) is length, area, or volume.  
This interpretation is the continuous analogue of the classical one.

---

### 1.5 How Kolmogorov’s Axioms Unify the Interpretations

Kolmogorov’s axioms do not favor any single interpretation.  
Instead, they provide a minimal set of rules that **all** consistent notions of probability must follow:

1) $$ P(A)\ge 0 $$  

2) $$ P(\Omega) = 1 $$

3) If $$ (A_1,A_2,\dots) $$ are disjoint, then  

$$
P\Bigl(\bigcup_i A_i\Bigr)=\sum_i P(A_i)
$$

On such:

- the classical interpretation becomes the special case of a finite uniform measure,  
- the frequentist interpretation becomes a statement about limits of empirical frequencies,  
- the Bayesian interpretation becomes a subjective measure satisfying coherence rules,  
- the geometric interpretation becomes a probability measure derived from geometric measure.

Thus all interpretations are *models* inside the same mathematical structure.

---

## 2. Probability and Measure Theory

Modern probability theory is built on measure theory.  
This allows us to handle infinite sample spaces, continuous random variables, and integration rigorously.

---

### 2.1 Sigma-Algebra

A sigma-algebra \(\mathcal{F}\) on \(\Omega\) is a collection of subsets closed under:

1. complements  
2. countable unions  
3. containing \(\Omega\) itself  

Events are the elements of \(\mathcal{F}\).  
Including only a sigma-algebra (not every possible subset) prevents contradictions and makes integration well-defined.

---

### 2.2 Probability Measure

A probability measure is a function

$$
P:\mathcal{F}\to[0,1]
$$

that satisfies Kolmogorov’s axioms.  
It assigns consistent probabilities to all events we care about.

---

### 2.3 Random Variables

A random variable is a measurable function

$$
X:\Omega\to\mathbb{R}.
$$

Measurability means that for any real number \(x\):

$$
\{\omega : X(\omega)\le x\}\in\mathcal{F}.
$$

This ensures that probabilities of events involving \(X\) are well-defined.

---

### 2.4 Distribution of a Random Variable

The distribution of \(X\) is the probability measure on \(\mathbb{R}\):

$$
\mu_X(B)=P(X\in B),
$$

for any Borel set \(B\subset\mathbb{R}$.

---

### 2.5 Expectation

The expectation of \(X\) is the Lebesgue integral

$$
\mathbb{E}[X]=\int_\Omega X(\omega)\,dP(\omega),
$$

which simultaneously handles discrete, continuous, and mixed random variables.

---

## 3. Derivations from the Axioms

Below are two fundamental consequences of the probability axioms.  
They are derived **entirely** from the axioms and basic set identities.

To make the reasoning clearer, the derivations are presented in **two parallel columns**:  
- **left:** algebraic steps,  
- **right:** commentary and conclusions.



# Simulation
<script src="https://cdn.plot.ly/plotly-latest.min.js"></script>

<style>
  .poisson-container {
      max-width: 900px;
      margin: auto;
      padding: 10px;
  }

  .input-bar {
      display: flex;
      gap: 16px;
      align-items: center;
      padding: 14px;
      border-radius: 10px;
      background: rgba(255,255,255,0.05);
      margin-bottom: 25px;
  }

  .input-group {
      display: flex;
      flex-direction: column;
      font-size: 14px;
  }

  input[type=number] {
      padding: 6px;
      border-radius: 6px;
      border: 1px solid #666;
      background: transparent;
      color: inherit;
      width: 90px;
  }

  button {
      padding: 8px 16px;
      border-radius: 8px;
      border: none;
      background: #4da3ff;
      cursor: pointer;
      font-weight: bold;
  }
  button:hover { opacity: .85; }

  .plot-box {
      margin-bottom: 40px;
      padding: 18px;
      background: rgba(255,255,255,0.06);
      border-radius: 14px;
  }

</style>

<div class="poisson-container">

<div class="input-bar">
    <div class="input-group">
        <label>λ (Lambda)</label>
        <input id="lambda_input" type="number" value="5" min="0.1" step="0.1">
    </div>

    <div class="input-group">
        <label>T</label>
        <input id="time_input" type="number" value="1" min="0.1" step="0.1">
    </div>

    <div class="input-group">
        <label>N (Steps)</label>
        <input id="n_input" type="number" value="5000" min="100" step="100">
    </div>

    <button onclick="updateAll()">Update All</button>
</div>


<!-- ================= TRAJECTORY ================== -->
<div class="plot-box">
    <h3>1. Trajectory N(t)</h3>
    <div id="traj"></div>
</div>


![Price Distribution](/assets/img/posts/2024-10-12-Datasets and Distrubtion/A.png)

<!-- ================ DISTRIBUTION ================= -->
<div class="plot-box">
    <h3>2. Distribution of N(T)</h3>
    <div id="histN"></div>
</div>

</div>

![Price Distribution](/assets/img/posts/2024-10-12-Datasets_and_Distrubtion/B.png)
<script>

function simulateCounts(lambda, T, steps) {
    let dt = T / steps;
    let t = 0;
    let N = 0;
    let times = [0];
    let counts = [0];

    for (let i=0; i<steps; i++) {
        if (Math.random() < lambda * dt) N++;
        t += dt;
        times.push(t);
        counts.push(N);
    }
    return {times, counts};
}

// ================= CLEAN TRAJECTORY =================

function plotTrajectory(lambda, T, steps) {
    const sim = simulateCounts(lambda, T, steps);
    const maxN = Math.max(...sim.counts);
    const yMax = Math.max(5, Math.ceil(maxN * 1.12));

    // clean, rounded tick for X (time)
    const idealTicks = 8;
    const raw = T / idealTicks;
    const mag = Math.pow(10, Math.floor(Math.log10(raw)));
    const norm = raw / mag;
    let nice;
    if (norm < 1.5) nice = 1;
    else if (norm < 3) nice = 2;
    else if (norm < 7) nice = 5;
    else nice = 10;
    const xTick = nice * mag;

    Plotly.newPlot("traj", [{
        x: sim.times,
        y: sim.counts,
        mode: "lines",
        line: { shape: "hv", width: 3 }
    }], {
        height: 380,
        xaxis: {
            title: "t",
            range: [0, T],
            dtick: xTick,
            gridcolor: "rgba(255,255,255,0.12)"
        },
        yaxis: {
            title: "N(t)",
            range: [0, yMax],
            dtick: Math.max(1, Math.round(yMax / 8)),
            gridcolor: "rgba(255,255,255,0.12)"
        },
        margin: { t: 30, r: 20, l: 60, b: 45 },
        paper_bgcolor: "rgba(0,0,0,0)",
        plot_bgcolor: "rgba(0,0,0,0)"
    });
}

// ================= DISTRIBUTION ===================

function factorial(n) { let f=1; for (let i=1;i<=n;i++) f*=i; return f; }

function poissonPMF(lambda, kmax) {
    let x=[], y=[];
    for (let k=0; k<=kmax; k++){
        x.push(k);
        y.push(Math.exp(-lambda)*(lambda**k)/factorial(k));
    }
    return {x,y};
}

function plotHistN(lambda, T, steps) {

    // Generate samples: final N(T)
    let samples = [];
    for (let i=0; i<steps; i++){
        let s = simulateCounts(lambda, T, steps);
        samples.push(s.counts[s.counts.length-1]);
    }

    const mean = lambda * T;

    // Max k to show
    const maxK = Math.max(...samples, Math.ceil(mean + 5 * Math.sqrt(mean)));

    // Bin into integer counts
    let freq = new Array(maxK + 1).fill(0);
    for (let v of samples) {
        if (v <= maxK) freq[v]++;
    }

    // Convert to density or absolute frequency (you wanted frequency)
    let xs = [...Array(maxK + 1).keys()];
    let ys = freq;

    // Poisson PMF curve (scaled to sample count)
    const pmf = poissonPMF(mean, maxK);
    const pmfScaled = pmf.y.map(v => v * steps);

    const yMax = Math.max(...ys, ...pmfScaled) * 1.15;

    // Clean integer bars
    const bars = {
        x: xs,
        y: ys,
        type: "bar",
        width: 0.9,                 // makes bars vertical without gaps
        marker: { color: "#4da3ff" },
        opacity: 0.65,
        name: "Simulated"
    };

    // Clean curve
    const curve = {
        x: pmf.x,
        y: pmfScaled,
        mode: "lines+markers",
        line: { width: 3, color: "#ffdd55" },
        marker: { size: 5, color: "#ffdd55" },
        name: "Poisson(λT)"
    };

    Plotly.newPlot("histN", [bars, curve], {
        height: 380,
        xaxis: {
            title: "k",
            dtick: 1,                        // ensures integer ticks
            gridcolor: "rgba(255,255,255,0.12)"
        },
        yaxis: {
            title: "Frequency",
            range: [0, yMax],
            gridcolor: "rgba(255,255,255,0.12)"
        },
        margin: { t: 30, r: 20, l: 60, b: 45 },
        barmode: "overlay",
        bargap: 0.05,                         // small gap, not scattered
        paper_bgcolor: "rgba(0,0,0,0)",
        plot_bgcolor: "rgba(0,0,0,0)"
    });
}

// ================= GLOBAL UPDATE =================

function updateAll() {
    const lambda = Number(document.getElementById("lambda_input").value);
    const T      = Number(document.getElementById("time_input").value);
    const steps  = Number(document.getElementById("n_input").value);

    plotTrajectory(lambda, T, steps);
    plotHistN(lambda, T, steps);
}

updateAll();

</script>


## Summary

Simulating a Bernoulli event in each of \( n \) small intervals with probability \( \lambda/n \) produces a discrete counting process that converges to a Poisson process with rate \( \lambda \). The limiting process has Poisson-distributed increments, independent increments, and exponentially distributed inter-arrival times.