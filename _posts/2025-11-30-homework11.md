---
title: Wiener Process (Brownian Motion) 
date: 2025-12-01 07:46 +0300
categories: [Stochastic Calculus, Cybersecurity]
tags: [Sample Paths, Distributions, Probability]
author: collinskirui
---

# 1.  Introduction
The **Wiener process**, also called [**standard Brownian motion**](https://hpaulkeeler.com/wiener-or-brownian-motion-process/) is a continuous-time stochastic process that displays random continuous “wiggly’’ behaviour. It is the mathematical model behind physical Brownian motion, diffusion, heat flow, stock-price modeling, and is the cornerstone of stochastic calculus.

In discrete form, one may think of Brownian motion as the limit of a symmetric random walk when the number of steps goes to infinity and the step size shrinks appropriately. This connection is formalized by the **Donsker invariance principle**, but here we focus on simulation and observable properties.

---

# 2.  Formal Definition

A stochastic process $$\{W(t),\, t \ge 0\}$$ is a **standard Wiener process** if:

1. **Initial condition:**  
   $$W(0) = 0$$

2. **Independent increments:**  
   For any ordered times  
   $$0 \le t_1 < t_2 < \cdots < t_n,$$  
   the increments $$W(t_k) - W(t_{k-1})$$ are independent.

3. **Gaussian increments:**  
   For all $$s > 0,$$  
   $$W(t+s) - W(t) \sim \mathcal{N}(0,\, s)$$

4. **Continuous paths:**  
   With probability 1, $$t \mapsto W(t)$$ is continuous.

From these axioms:

i. $$\mathbb{E}[W(t)] = 0$$  
ii. $$\operatorname{Var}[W(t)] = t$$  
iii. $$W(t) \sim \mathcal{N}(0,t)$$  

The corresponding transition density is:

$$
f_{W(t)}(x) = \frac{1}{\sqrt{2\pi t}}
\exp\!\left(-\frac{x^2}{2t}\right)
$$

---

# 3. Key Properties
---

## 3.1 Stationary and Gaussian increments

If $$\Delta W = W(t+h) - W(t),$$ then:

$$
\Delta W \sim \mathcal{N}(0, h)
$$

independent of the starting time.

---

## 3.2 Scaling property

For any constant $$c > 0$$:

$$
W(ct) \stackrel{d}{=} \sqrt{c}\, W(t).
$$

---

## 3.3  Continuity but nowhere differentiability

Sample paths are continuous everywhere but almost surely nowhere differentiable — they oscillate too wildly.

---

## 3.4 Martingale property

$$
\mathbb{E}[W(t)\mid\mathcal{F}_s] = W(s) \quad (s < t)
$$

---

## 3.5 Markov property

$$
\mathbb{P}(W(t+h)\in A \mid \mathcal{F}_t)
=
\mathbb{P}(W(t+h)\in A \mid W(t)).
$$

---

# 4. Simulation via Discretization

Let $$[0,1]$$ be divided into $$N$$ steps:

$$
\Delta t = \frac{1}{N}.
$$

Brownian increments:

$$
\Delta W_i = \sqrt{\Delta t}\, Z_i, \qquad Z_i \sim \mathcal{N}(0,1).
$$

Path update:

$$
W_{i+1} = W_i + \Delta W_i.
$$

As $$N \to \infty$$ this converges to true Brownian motion.

---

# 5. Results: Sample Paths and Distributions

## 5.1 Simulated Brownian Paths
- The paths begin at zero, wander continuously, and exhibit variance that increases with time matching the theoretical property $$\operatorname{Var}[W(t)] = t$$.

![Simulated_Brownian_Paths](/assets/img/posts/2024-10-12-Datasets and Distrubtion/brownian motion.png)

---

## 5.2 Histogram of Increments

We examine:

$$
\frac{W(t+\Delta t)-W(t)}{\sqrt{\Delta t}}.
$$

These should follow $$\mathcal{N}(0,1).$$

![Histogram](/assets/img/posts/2024-10-12-Datasets and Distrubtion/histogram brownian.png)

---

## 5.3 Variance Growth Check

Theoretical:

$$
\operatorname{Var}[W(t)] = t.
$$

Empirical variance grows linearly, matching theory.

![variance](/assets/img/posts/2024-10-12-Datasets and Distrubtion/var_growth.png)


---

# 6. Simulating Brownian Motion Using Euler–Maruyama

The implementation of the **Euler–Maruyama (EM)** method for general SDEs:

$$
dX_t = a(X_t,t)\,dt + b(X_t,t)\,dW_t.
$$

![Euler–Maruyama](/assets/img/posts/2024-10-12-Datasets and Distrubtion/euler_bm.png)

For Brownian motion:

- Drift: $$a(x,t)=0$$  
- Diffusion: $$b(x,t)=1$$  

Thus the EM update becomes:

$$
W_{n+1} = W_n + \sqrt{\Delta t}\, Z_n, \qquad Z_n \sim \mathcal{N}(0,1).
$$

which exactly matches the discretization used earlier.

---
# 6.1 Simulation Tool

<div id="sde-box" style="max-width:900px;margin:auto;">
  <h2 style="font-family:Arial;margin-bottom:8px;">Interactive SDE Simulator</h2>

  <label>SDE:</label>
  <select id="sde-select">
    <option value="wiener">Wiener: dX = dW</option>
    <option value="ou">OU: dX = θ(μ−X)dt + σ dW</option>
    <option value="gbm">GBM: dX = μXdt + σXdW</option>
  </select>

  <label style="margin-left:10px;">Paths:</label>
  <input id="num-paths" type="number" value="5" min="1" max="20">

  <label style="margin-left:10px;">θ, μ, σ:</label>
  <input id="params" type="text" value="1,0,0.3">

  <button onclick="resetSim()" style="margin-left:10px;">Restart</button>
</div>

<div id="sde-canvas"></div>

<script src="https://cdn.jsdelivr.net/npm/p5@1.6.0/lib/p5.min.js"></script>

<script>
let t = 0, dt = 0.004;
let paths = [];
let params, type;

function drift(x) {
  if(type==="wiener") return 0;
  if(type==="ou") return params.th*(params.mu - x);
  if(type==="gbm") return params.mu * x;
}

function diff(x) {
  if(type==="wiener") return 1;
  if(type==="ou") return params.sigma;
  if(type==="gbm") return params.sigma * x;
}

function resetSim() {
  t=0;
  let [th,mu,sigma] = document.getElementById("params").value.split(",").map(Number);
  params = {th,mu,sigma};
  type = document.getElementById("sde-select").value;

  let n = parseInt(document.getElementById("num-paths").value);
  paths = Array.from({length:n}, () => [0]);
}

function setup() {
  let c = createCanvas(900,430);
  c.parent("sde-canvas");
  textFont("Arial");
  resetSim();
}

function drawGrid() {
  stroke(225);
  strokeWeight(1);
  for(let x=60; x<width-20; x+=80) line(x,20,x,height-60);
  for(let y=20; y<height-60; y+=60) line(60,y,width-20,y);
}

function drawAxes() {
  stroke(120);
  strokeWeight(1.4);
  line(60,height-60,width-20,height-60);
  line(60,20,60,height-60);

  noStroke();
  fill(60);
  textSize(15);

  text("time (t)", width/2-20, height-25);

  push();
  translate(25, height/2+20);
  rotate(-HALF_PI);
  text("X(t)", 0,0);
  pop();
}

function draw() {
  background(255);

  drawGrid();
  drawAxes();

  let colors = [
    "#1f77b4", "#ff7f0e", "#2ca02c",
    "#d62728", "#9467bd", "#8c564b"
  ];

  for(let p=0; p<paths.length; p++){
    let A = paths[p];
    let x = A[A.length-1];

    let z = randomGaussian();
    x = x + drift(x)*dt + diff(x)*sqrt(dt)*z;
    A.push(x);

    stroke(colors[p % colors.length]);
    strokeWeight(1.8);
    noFill();

    beginShape();
    for(let i=0; i<A.length; i++){
      let px = map(i,0,1/dt,60,width-20);
      let py = map(A[i],-3,3,height-60,20);
      vertex(px,py);
    }
    endShape();
  }

  t+=dt;
}
</script>



<!--  MATHS  -->
<script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
<script>
window.MathJax = {
  options: {
    skipHtmlTags: ['script', 'noscript', 'style', 'textarea', 'pre'],
    ignoreHtmlClass: 'mathjax-ignore'
  }
};
MathJax.typesetPromise([document.body]).catch(function (e) {
  console.error(e);
});
</script>


